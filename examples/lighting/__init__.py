"""The lighting tutorial series: build_scene(w, h)/configure_viewer(viewer, root) walkthroughs
from a bare Lambert term through PBR/IBL, shadows, and dynamic probes. Run one via
``python -m OpenSceneGraph.examples lighting.lambert`` or directly as
``python -m OpenSceneGraph.examples.lighting.lambert``.
"""
