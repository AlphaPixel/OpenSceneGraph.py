# TODO

[x] GUIEventHandler (event dispatch override)
[x] NodeCallback (scene graph callback override) Important and similarly tricky
[ ] Drawable::DrawCallback Also tricky due to rendering pipeline and non-copyable arguments
[ ] CameraManipulator::handle() Multimethod override, multiple overloads, non-copyable args
[ ] Operation / UpdateCallback patterns
